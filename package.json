{
  "name": "obra-vds-vercel-static",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vercel dev",
    "build": "npm run build --prefix frontend/_frontend && node scripts/build-static.js",
    "build:next": "npm run build --prefix frontend/_frontend",
    "build:static": "node scripts/build-static.js",
    "db:init": "node scripts/init-db.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
