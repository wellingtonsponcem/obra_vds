{
  "version": 2,
  "installCommand": "npm ci && npm ci --prefix frontend/_frontend",
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/[...path].js"
    }
  ]
}
